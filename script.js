/* var prevScrollpos = window.pageYOffset;
window.onscroll = function() {
    var currentScrollPos = window.pageYOffset;

    if (currentScrollPos > 20) {
        // Adjust the top position to hide the navigation bar
        document.querySelector(".top").style.top = "-80px"; // Adjust the height of your navigation bar accordingly
    } else {
        // Reset the top position to show the navigation bar
        document.querySelector(".top").style.top = "0";
    }

    prevScrollpos = currentScrollPos;
} */